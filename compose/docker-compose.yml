services:
  app:
    build: ../app
    ports:
      - "5000:5000"
    environment:
      - APP_ENV=production
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
